# LINE Token Deployment Environment (TESTNET)
# Copy to .env.deploy and fill in values
# NEVER commit .env.deploy with real keys

# Vara Testnet RPC
VARA_TESTNET_RPC=wss://testnet.vara.network

# Deployer key - NEVER commit real value
# Use KMS or CI secrets to inject this
DEPLOYER_PRIVATE_KEY=DEPLOYER_PRIVATE_KEY_PLACEHOLDER

# Token initialization
LINE_CONTRACT_INIT_NAME="LINE"
LINE_CONTRACT_INIT_SYMBOL="LINE"
LINE_DECIMALS=9

# After deployment, save the contract address here
# LINE_TOKEN_CONTRACT_ADDRESS=

# Backend minter address (add as minter after deploy)
# MINTER_ADDRESS=
